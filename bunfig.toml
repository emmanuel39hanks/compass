[install]
# Use bun workspaces
workspaces = true

[test]
# Only discover tests from package src dirs. Local/integration tests (real
# API calls, mainnet relays) live under test/local/ and run separately.
root = "packages"
